原型设计模式应用于对象的copy，一般在一个对象构造较为困难时使用，例如有参构造器需要五秒的时间进行构造，此时使用克隆方法仅需一瞬间即可完成克隆
克隆分为深克隆、浅克隆，浅克隆只能复制一份基本数据类型和String类型，在改变原对象时，新对象的基本数据类型和String类型数据不会跟着改变，而引用
数据类型会跟着改变。

深克隆有两种实现方法，1、重写clone()方法。2、使用序列化、反序列化。可以是对象的基本数据类型、引用数据类型完完全全复制一份，再没有任何联系。
重写客隆方法一般为一下步骤:
1、Object obj = super.clone();
2、将obj强转为原类型
3、将obj的引用数据类型（不包括String）也进行克隆，并赋值给属性。
4、返回obj